package com.zikre.bazmeauliya.ui.theme

import androidx.compose.ui.graphics.Color

// 🌙 Light Theme Colors
val PrimaryLight = Color(0xFF2E7D32)       // Emerald green — spiritual and calming
val SecondaryLight = Color(0xFF8D8D8D)     // Muted gray — neutral support
val TertiaryLight = Color(0xFFD4AF37)      // Soft gold — accent warmth
val BackgroundLight = Color(0xFFF9F9F9)    // Clean off-white
val SurfaceLight = Color(0xFFFFFFFF)       // Pure white surface
val ErrorLight = Color(0xFFB00020)         // Muted red for validation

// 🌌 Dark Theme Colors
val PrimaryDark = Color(0xFF81C784)        // Soft green — gentle on dark background
val SecondaryDark = Color(0xFFB0B0B0)      // Light gray — readable in dark mode
val TertiaryDark = Color(0xFFFFD700)       // Bright gold — spiritual accent
val BackgroundDark = Color(0xFF121212)     // Deep night — meditative
val SurfaceDark = Color(0xFF1E1E1E)         // Slightly lighter surface
val ErrorDark = Color(0xFFCF6679)          // Soft error tone